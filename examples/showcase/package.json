{
  "name": "@sukuna/example-showcase",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "bun run build:client && bun run build:ssr && bun run prerender",
    "build:client": "vite build",
    "build:ssr": "vite build --ssr src/entry-server.tsx --outDir dist-ssr",
    "prerender": "bun run scripts/prerender.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "sukuna-ui": "link:sukuna-ui",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4",
    "@vitejs/plugin-react": "^4.3.0",
    "tailwindcss": "^4",
    "vite": "^7.0.0"
  }
}
