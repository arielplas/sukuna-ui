---
"sukuna-ui": minor
---

Accordion: new `headingLevel` prop (1–6, default 3) so each item's header renders as the right
`<h1>`–`<h6>` for your document outline (WCAG 1.3.1), with the trigger nested inside the heading.
