---
"sukuna-ui": patch
---

RadioGroup: clicking an option's label text now selects it, not just the radio circle. The whole
row is the control; the accessible name (via `aria-labelledby`) and visuals are unchanged.
