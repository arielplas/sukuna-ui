# Normalize line endings to LF in the repo; check out as-is on the OS.
* text=auto eol=lf

# Binary / generated — never diff or touch line endings.
bun.lock -text
*.png binary
*.jpg binary
*.woff2 binary
